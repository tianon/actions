# `uses: tianon/actions/setup-go@HEAD`

"Are we there yet?"

"No, but we're H O T T O **G O**"

---

"Why does setting up Go require Node.js?"

It doesn't.

(a very simplified clone of [GitHub's `actions/setup-go`](https://github.com/actions/setup-go) but without all that wild Node.js)
